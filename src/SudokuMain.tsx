import * as React from 'react';

export default class SudokuMain extends React.Component<{}, {}> {
    render () {
        return (
            <div id="container">
            </div>
        );
    }
}
